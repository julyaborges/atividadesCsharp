/*using System;

// Quantidade de números que serão lidos
Console.WriteLine("Digite um valor entre 3 e 10:");
int quant = int.Parse(Console.ReadLine());

// Inicia soma e média com 0
double soma = 0;
double media = 0;

// Vetor double com o tamanho recebido
double[] numeros = new double[quant];

// Insere os valores no vetor e soma os números
for (int i = 0; i < numeros.Length; i++)
{
    Console.WriteLine("Insira o valor "+(i+1)+":");
    numeros[i] = double.Parse(Console.ReadLine());
    soma += numeros[i];
}

// Calcula a média
media = soma/quant;

// Exibe os resultados 
Console.WriteLine("Soma:"+soma);
Console.WriteLine("Média:"+media);
*/